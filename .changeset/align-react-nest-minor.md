---
"@tonightpass/react": minor
"@tonightpass/nest": minor
---

Align `@tonightpass/react` and `@tonightpass/nest` to the `0.1.x` line alongside `tonightpass`, so the whole ecosystem shares the same minor going forward.
