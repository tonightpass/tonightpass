---
"tonightpass": patch
"@tonightpass/react": patch
"@tonightpass/nest": patch
---

Add SSE support for live event view counts: `OrganizationEventViewResult` type, `getStreamUrl` SDK method, `useAPISubscription` React hook, and `@Sse` type augmentation for NestJS
