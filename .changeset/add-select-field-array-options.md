---
"tonightpass": patch
---

Add `select` field to `ArrayOptions` type for field selection in array queries, replacing the previous commented-out `fields`/`exclude` approach. Supports dot notation for nested relations.
