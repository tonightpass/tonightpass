---
"tonightpass": patch
---

Add `Place` type and `PlaceEndpoints` for the places/cities API (`GET /places`, `GET /places/:countrySlug/:citySlug`, `GET /places/search`), and SDK client `places.getAll()`, `places.get()`, `places.search()`.
